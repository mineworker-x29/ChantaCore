# CLI Reference

This reference documents help/report-oriented public alpha surfaces only.

Documented groups: alpha contract, alpha hygiene, alpha packaging, alpha boundary, alpha schumpeter, alpha schumpeter-prep, alpha runtime, alpha smoke, alpha docs, alpha examples, and memory consolidation.

The CLI reference does not document provider invocation or command execution expansion as enabled.
