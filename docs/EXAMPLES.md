# Examples

The v0.28.7 example pack is synthetic and deterministic. Example pack is not production dataset.

Example manifests cover synthetic OCEL demo metadata, smoke CLI report surfaces, and safety boundary report surfaces. Examples do not contain actual user data, company data, credentials, secrets, private identifiers, raw traces, raw transcripts, or raw provider outputs.
