# Workbench Foundation

Workbench documentation covers trace explorer, provider browser preview, evidence inspector, safety approval console preview, run dashboard, command surface preview, and snapshot export preview.

These are report/preview surfaces. The guide does not claim provider invocation or command execution.
